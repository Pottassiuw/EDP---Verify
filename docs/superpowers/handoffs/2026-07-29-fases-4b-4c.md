# Handoff — Fases 4b e 4c da Carteira

Data: 2026-07-29
Branch: `develop`

## Objetivo confirmado

Continuar as duas próximas fatias da Carteira:

- **Fase 4b:** enriquecimento read-only dos detalhes de notas do Input e
  COFFEE com dados já projetados em `nota_carteira`.
- **Fase 4c:** migração visual do legado `.edp-*` para a direção Supabaze,
  dividida em fundação e lotes por feature.

## Estado do trabalho

Brainstorming iniciado e escopo macro confirmado pelo usuário. Nenhum código
das fases 4b/4c foi implementado ainda.

Specs existentes:

- `docs/superpowers/specs/2026-07-29-carteira-fase-4b-enriquecimento-design.md`
- `docs/superpowers/specs/2026-07-29-carteira-fase-4c-migracao-visual-supabaze-design.md`

O Graphify confirmou os nós `Carteira Fase 4b`, `Carteira Fase 4c`,
`Enriquecimento Databricks` e `Migração Visual Supabaze`. Seus artefatos
portáveis estão em `graphify-out/`.

## Próxima conversa

Seguir o skill `superpowers:brainstorming`, com uma pergunta por vez.
Primeira decisão pendente:

> Na Fase 4b, confirmar o join por `Numero_Nota` ↔ `id_sap`, limitado a
> `sap_real=1`, com enriquecimento apenas no detalhe e sem `notas_sp`.

Depois, confirmar:

1. conjunto de campos read-only;
2. comportamento para ausência, tombstone e base não sincronizada;
3. ordem dos lotes da Fase 4c;
4. abordagem de tokens globais Supabaze.

A Fase 4c é grande e não deve virar um único commit. Divisão prevista:
`4c-0` fundação, `4c-1` Carteira, `4c-2` Relatórios, `4c-3` Input,
`4c-4` COFFEE, `4c-5` Verificar e `4c-fim` limpeza.

## Como retomar em outro computador

1. Clonar o repositório ou executar `git pull origin develop`.
2. Abrir a pasta no Codex.
3. Iniciar uma nova task com:

   `Leia AGENTS.md e docs/superpowers/handoffs/2026-07-29-fases-4b-4c.md.
   Continue o brainstorming das fases 4b e 4c a partir da primeira decisão
   pendente.`

4. Não reconstruir o Graphify antes de tentar `graphify query`; o grafo
   portável já está versionado.
