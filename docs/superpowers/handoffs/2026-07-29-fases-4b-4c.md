# Handoff — Fases 4b e 4c da Carteira

Data: 2026-07-29
Branch: `develop`

## Objetivo confirmado

Continuar as duas próximas fatias da Carteira:

- **Fase 4b:** enriquecimento read-only dos detalhes de notas do Input e
  COFFEE com dados já projetados em `nota_carteira`.
- **Fase 4c:** migração visual do legado `.edp-*` para a direção Supabaze,
  dividida em fundação e lotes por feature.

## Estado do trabalho

Brainstorming concluído e design das duas fases aprovado pelo usuário. Nenhum
código das fases 4b/4c foi implementado ainda.

Specs existentes:

- `docs/superpowers/specs/2026-07-29-carteira-fase-4b-enriquecimento-design.md`
- `docs/superpowers/specs/2026-07-29-carteira-fase-4c-migracao-visual-supabaze-design.md`

Decisões confirmadas:

- **4b:** join por `Numero_Nota`/`coffee.id_sap` ↔ `nota_carteira.id_sap`,
  somente `sap_real=1`; nove campos read-only; card hierárquico somente nos
  detalhes; estados distintos para sem correspondência, tombstone e base não
  sincronizada; `notas_sp` fora da fase.
- **4c:** Supabaze global em `:root` desde `4c-0`; `.edp` como compatibilidade
  temporária; lotes sequenciais na ordem Carteira → Relatórios → Input →
  COFFEE → Verificar; limpeza somente com zero referências legadas.
- **Entrega:** concluir 4b-backend e 4b-frontend antes de iniciar 4c.

O Graphify confirmou os nós `Carteira Fase 4b`, `Carteira Fase 4c`,
`Enriquecimento Databricks` e `Migração Visual Supabaze`. Seus artefatos
portáveis estão em `graphify-out/`.

## Próxima conversa

O usuário deve revisar as duas specs atualizadas. Após aprovação explícita,
seguir `superpowers:writing-plans` e criar primeiro os planos sequenciais da
4b. A 4c permanece decomposta em `4c-0`, `4c-1`, `4c-2`, `4c-3`, `4c-4`,
`4c-5` e `4c-fim`; não criar um plano ou commit monolítico.

## Como retomar em outro computador

1. Clonar o repositório ou executar `git pull origin develop`.
2. Abrir a pasta no Codex.
3. Iniciar uma nova task com:

   `Leia AGENTS.md e docs/superpowers/handoffs/2026-07-29-fases-4b-4c.md.
   Continue a partir da revisão das specs aprovadas das fases 4b e 4c.`

4. Não reconstruir o Graphify antes de tentar `graphify query`; o grafo
   portável já está versionado.
